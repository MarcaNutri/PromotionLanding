import react from 'react'
import styles from './style.module.scss';

const NavBar = ( ) => {
  return (
    <div className={`${styles.navbar_container} rounded-2xl bg-white w-10/12 text-orange-600`}>
      navBar
    </div>
  )
}

export default NavBar
